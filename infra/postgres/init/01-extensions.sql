-- Extensiones requeridas por Reforma Hub.
-- Este script solo se ejecuta la primera vez que se crea el volumen de datos.
CREATE EXTENSION IF NOT EXISTS postgis;
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
